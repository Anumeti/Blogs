# include <stdio.h>
int main()
{   //This the code for perimeter of rectangle

    int length;
    printf("write the length");
    scanf("%d",&length);

    int width;
    printf("write the width");
    scanf("%d",&width);

    printf("The perimeter of triangle is : %d\n", (length+width)*2 );
    return 0;

    }